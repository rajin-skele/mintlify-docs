---
title: "Border Width"
description: "The bundled border-width scale from `border-0` through `border-20`."
---

import { BorderWidthDemo } from "/snippets/border-width-demo.jsx"

## Interactive Demo

<BorderWidthDemo />

## Border Width Scale

The bundled border-width classes are:

| Class | Value | CSS | Usage |
| ----- | ----- | --- | ----- |
| `.border-0` | 0 | `border-width: 0;` | No border |
| `.border-1` | 1px → 1.25px | `var(--border-1)` | Subtle separation |
| `.border-2` | 2px → 2.5px | `var(--border-2)` | Standard border |
| `.border-4` | 4px → 5px | `var(--border-4)` | Accent framing |
| `.border-8` | 8px → 10px | `var(--border-8)` | Strong decorative border |
| `.border-10` | 10px → 12.5px | `var(--border-10)` | Heavy framing |
| `.border-15` | 15px → 18.75px | `var(--border-15)` | Display framing |
| `.border-20` | 20px → 25px | `var(--border-20)` | Maximum emphasis |

```html
<div class="card border-2 border-solid border-gray">
  <h3 class="text-xl font-semibold text-black mb-2">Standard border</h3>
  <p class="text-gray">Use the bundled border classes directly.</p>
</div>
```

## Common Patterns

```html
<input
  type="text"
  class="w-full p-3 border-1 border-solid border-gray rounded"
  placeholder="Normal input"
>
```

```html
<div class="border-4 border-solid border-blue rounded-lg p-6 bg-white">
  <h3 class="text-lg font-semibold mb-2">Accent border</h3>
  <p class="text-gray">Heavier widths work best for emphasis and framing.</p>
</div>
```

## Best Practices

- Use `border-1` and `border-2` for most UI surfaces.
- Use `border-4` and above sparingly for accents, highlights, and decorative frames.
- Pair width utilities with explicit style and color helpers such as `.border-solid` and `.border-gray`.
- Keep border widths consistent across similar components.
